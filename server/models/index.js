/* Este nuevo archivo es solo para organizar tus modelos y facilitar su importación. 
No reemplaza ni interfiere con el otro index.js. */

const Product = require('./Product');
const Cart = require('./Cart');
const CartItem = require('./CartItem');

module.exports = {
  Product,
  Cart,
  CartItem,
};
